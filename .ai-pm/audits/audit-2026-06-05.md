# Protocol audit — 2026-06-05

## Summary

Full-scope sweep of the entire project history — all 7 substrate features (`webdav-transport`, `crypto`, `identity`, `message-model`, `sync`, `directory`, `chat-directory`), the `release-ci` infra feature, and the `v2.23.0 → v2.25.1` protocol bump. Protocol health is **strong: zero blocking findings.** Every merged feature has a plan, a review stamped `## Code review: <date> — passed` with an `approve` Verdict and a `DoD: pass`, and the security-touching features carry a fully wired threat-model. `SCn` integrity is exact across the whole history (`SC1`–`SC20` all defined and all referenced, no dangling, no orphan). The protocol bump triggered **no** pending structural migration. The full sweep confirms the prior same-day diff run's two notes and adds **one** additional non-blocking note the diff window did not reach: a stale count phrase in the product-map intro. No earlier substrate accumulated a gap.

## Contract check

Subject of the first sentence of scenario 1, extracted from each plan directly. Every plan opens its Scenarios section with an explicit **system-subject / no-UI disclaimer** ("System behaviors (no UI yet …)", "Backend/infrastructure", "Backend substrate — no UI", "System-subject"). Two earlier plans (`identity`, `sync`) and the two directory plans use a human-role *token* ("a user" / "A member") inside an explicitly-disclaimed system-subject scenario set — the actor is the engine, no user-visible surface ships. This is the same framing the prior audits applied; the no-contract / no-advocate outcome is correct.

| Feature | Scenario 1 subject | Needs contract | Contract file |
|---|---|---|---|
| webdav-transport | "The app is configured … and establishes a connection" — the app / system | no | n/a |
| crypto | "The substrate can obtain a per-chat symmetric key three ways" — the substrate / system | no | n/a |
| identity | "On first run **a user** gets a stable identity" — human-role token, but plan states "No UI. Backend/infrastructure."; the actor is the identity engine, no surface ships | no (disclaimed) | n/a |
| message-model | "A **text message** can be built with …" — a message / object, system | no | n/a |
| sync | "A **member** sends a message; it is written once to the chat's shared log" — human-role token, but plan states "Backend substrate — no UI … behavior is exercised by tests, not a screen" | no (disclaimed) | n/a |
| directory | "A **member** publishes their identity entry" — illustrative; backend substrate, no UI | no (disclaimed) | n/a |
| chat-directory | "A **member** publishes a chat's discovery descriptor" — illustrative; backend substrate, group-only, no UI | no (disclaimed) | n/a |
| release-ci | "When a pull request targets `main`, a GitHub Actions workflow checks out the code" — the workflow / a push / a release, never a human role | no | n/a |

No feature requires a Product Contract, and `.ai-pm/contracts/` does not exist — correct, not a gap (all features are non-user-facing backend/infra). There are no `yes` + `MISSING` rows → no blocking finding from the contract sweep.

## Blocking

None.

## Notes

1. `docs/architecture.md` `## Behavioral contract (taxonomies & invariants)` — **no `### System invariants` index subsection.** The project satisfies the gate for one: it has `SC1`–`SC20` in `## Security constraints` **and** a journey `**Invariants:**` block in `docs/user-journeys.md` (line 28). A single `### System invariants` entry point that indexes the scattered invariants is the expected discipline; it is absent. The invariants themselves are present and well-homed (`### Shared-log / change-index / file-naming invariants`, `### Ordering & causality`, the journey block, SC18–SC20) and every `SCn` reference resolves cleanly — so this is a missing *index*, not a missing invariant. **Why it matters:** without one entry point, a future reader or planner has no single place to see "what must always hold" and may re-derive an invariant that already exists. **Remediation:** spawn `pm-architect` to add a `### System invariants` index to the Behavioral contract (index the existing invariants by `SCn` / subsection reference; do not restate them). Note, not blocking (docs may lag; conditional/additive discipline). This is the same note the same-day diff run raised — second consecutive surfacing; if it persists into the next full audit it upgrades to blocking.

2. `docs/product-map.md` (intro line) — **stale count phrase.** The generated intro says "**Six** backend substrates have landed," but the Infrastructure table beneath it correctly lists **eight** features (the seven substrates `webdav-transport` / `crypto` / `identity` / `message-model` / `sync` / `directory` / `chat-directory`, plus the `release-ci` infra feature). The load-bearing content — the feature table with Done dates and review links — is complete and correct; only the prose count in the generated preamble lagged behind the last two landings. **Why it matters:** a PM reading the map's summary line gets a wrong feature count; the map is auto-generated, so a stale preamble signals the generation step ran against an older feature set. **Remediation:** regenerate `docs/product-map.md` from source (idempotent overwrite) so the intro count matches the table. Note, not blocking. (This note is full-scope-only — the same-day diff window did not compare the intro count against the whole-history feature set.)

3. `docs/features/identity_plan.md` / `sync_plan.md` / `directory_plan.md` / `chat-directory_plan.md` — the **grammatical subject of scenario 1 is a human-role token** ("a user" / "A member") on a non-user-facing backend substrate. Each plan resolves this explicitly (system-subject / no-UI disclaimer in the scenario preamble; reviews and state files record that the advocate gate does not fire), so the no-contract / no-advocate outcome is **correct** — recorded only for transparency, because a literal first-sentence extraction would mis-flag these. **Why it matters:** the exemption rests on a disclaimer sentence rather than a system-subject scenario; a future substrate that drops the disclaimer could slip the gate. **Remediation:** none now; when the chat surface / discovery UI feature lands, that feature *is* user-facing and must carry both a Product Contract and a resolved advocate artifact. Note, not blocking.

## What looks healthy

- **Artifact completeness — clean across all 8 features.** Every feature has a plan and a review; every review is stamped `## Code review: <date> — passed` (webdav-transport / crypto / identity / message-model / sync / directory / chat-directory 2026-06-04, release-ci 2026-06-05) with an `approve` Verdict and `DoD: pass`. No unstamped trail, no `NOT YET RUN` remnant, no missing artifact. Software project (no `## Project kind` line → safe `software` default), so no `## Validation` section is expected and none is mis-stamped.
- **Plan ↔ implementation parity — clean.** Each review confirms all scenarios + interaction scenarios + stack-spec tests implemented and tested. The `sync` review records that the prior `request-changes` Pass-1 (two missing concurrency interaction tests) was resolved (commit `4988182`) before the `approve` — the review loop worked as designed and the audit re-confirmed the stamped result.
- **Implementation ↔ plan parity — clean.** All 7 `feat` commits map to the 7 substrate/feature plans; `release-ci` is a `ci:` commit with its own plan. No orphaned implementation, no `feat:`-on-main without a plan.
- **Security wiring — clean across the whole history.** `SC1`–`SC20` are all defined in `## Security constraints` and all referenced in the threat-model Mitigation column — **no dangling `SCn`, no orphan constraint.** The directory substrates' rows T20–T25 are wired to SC18/SC19/SC20/SC1 exactly as the task describes. The `## Realized by — feature → SCn / Threat traceability` table backfills the four pre-threat-model substrates (the 2026-06-04 sequencing artifact) and is consistent. Threat-model is **populated** (25 threat rows, no `<placeholder>` tokens). `Last reviewed: 2026-06-04` matches the latest security-touching merge (`chat-directory`, 2026-06-04) — **not stale**. `release-ci` is infra (CI), correctly not security-touching and not listing a threat-model update.
- **Autonomous-mode escalation — correct.** `chat-directory` carries `Decision authority: autonomous`; the group-only / DMs-excluded scope decision was an **escalated** security-relevant fork (PII / social-graph), recorded in the plan's scope-decision note (the proper home), surfaced in SC20 + T23. The substrate is non-user-facing, so the advocate did not fire — no confabulation, no missing advocate artifact.
- **Infra-row compliance — clean.** `release-ci` skips the Product Contract with an honest one-line reason; non-human-role subjects → no advocate required.
- **Docs currency — strong.** `docs/product.md` carries the **English** funnel headers (`## Why this exists` / `## What it does today` / `## Documents` / `## Features`) → pass; no wire-token shapes in the funnel body. `README.md` has **no** `## What it does` capability section → front-gate-migrated, not flagged. `docs/user-journeys.md` has an `**Invariants:**` block and correctly **references** `## Behavioral contract (taxonomies & invariants)` by name (move-not-copy) — the reference resolves (the section exists), and the journey body is plain human language with no backticked declared-taxonomy tokens and no wire-token shapes, so the journey-restatement / dangling-reference checks all pass. The architecture module map is fully reconciled against the real tree (every landed package "Implemented", `.github/workflows/` row present).
- **Product-map structure — clean.** Contract-less / infra-only map (no `Guarantees:` / `Что даёт:` labels, no value lines) → **not** old-format, no format-refresh note. Every plan is rendered (all 8 in the Infrastructure bucket with correct Done dates and resolving review links); no contract is stale-rendered (none exist). Only the intro count drifted (Note 2).
- **Protocol bump — no migration triggered.** Submodule at v2.25.1 (`fc2faec`). `### Pending-migration detection` finds nothing un-migrated: value-first/infra-only product-map, English product.md funnel, no `docs/features/_index.md`, no `## What it does` README section, no stale `## Project kind: process` line, no token-laden contracts (none exist), no unstamped review trails. The v2.24–v2.25 disciplines (state-model section, automode procedural gates, NFR limits) are additive and conditional — state-model is judgment-gated and silent unless the orchestrator deems a feature state-bearing, so its absence is below the note bar; the sync cursor / Room history lifecycle is already well-described in the module map + Behavioral contract.
- **Doc / state hygiene — clean.** `.ai-pm/state/current.md` is `Status: idle` with all work archived; both target archive files (`chat-directory-2026-06-04.md`, `release-ci-2026-06-05.md`) present, alongside the five earlier substrate archives. The deflake (`e8772c1`) and the protocol bump (`55efbbf`) are both recorded in the state Done section.
- **Flaky-test deflake — correctly trivial.** `e8772c1` is test-only (9 lines in `WebDavConcurrencyTest.kt` + 1 state line, LAZY coroutine start fix); no production change, no plan/contract needed, no protocol gap.

## Priority order for remediation

1. **Note 1** — `pm-architect` adds a `### System invariants` index to `docs/architecture.md` `## Behavioral contract`. The one actionable structural item; it is the expected-discipline gap the newer template surface introduces, it is now on its **second consecutive surfacing**, and it is cheap (index the existing invariants by `SCn` / subsection reference, do not restate them).
2. **Note 2** — regenerate `docs/product-map.md` from source so the intro "Six backend substrates" count matches the eight-row table. Mechanical, idempotent.
3. **Note 3** — no action now; carry forward as the reminder that the eventual chat-surface / discovery UI feature is user-facing and must carry a Product Contract + a resolved advocate artifact.
