# Execution state

Single source of truth for the currently active task. Overwritten as the task progresses; archived to `.ai-pm/state/archive/<topic>-<YYYY-MM-DD>.md` on completion.

PM reads this when curious about progress; PM never edits it. Agents read it as their first step and update it as their last step.

---

## Task

crypto — E2E encryption substrate: 3 key sources (passphrase Argon2id / random CSPRNG / known-from-chat-id), XChaCha20-Poly1305 AEAD with 8-byte header as AAD, Android Keystore wrapping. Fills the envelope ciphertext slot. Backend-only.

## Status

coding (Pass-2 review hygiene fixes applied; JVM pipeline green; connectedAndroidTest still blocked by MIUI INSTALL_FAILED_USER_RESTRICTED on 5c3ff0 → device needs install permission, then re-run)

## Done

- Plan approved (after extended threat-model discussion), saved to docs/features/crypto_plan.md
- Product map updated (crypto in Infrastructure bucket)
- Branch feature/crypto created
- pm-architect: filled webdav-layout.md §5/§5.1 crypto decisions + revised chat taxonomy + recorded decision 9 + follow-ons
- pm-coder: added lazysodium-android 5.1.0 + lazysodium-java 5.1.0 + JNA 5.13.0 (@aar app / jar test); implemented crypto/ (ChatKey, NativeCrypto iface, LazySodiumCrypto adapter, Aead seal/open §5.1, KeySources 3 sources, MessageCrypto envelope integration, CryptoFactory android) + keystore/ (ChatKeyStore AES/GCM Keystore wrap); 23 JVM tests (roundtrip/AAD/tamper/wrong-key/nonce/determinism/truncation + 3 stack-spec) all green; 6 instrumented tests written (native load, AEAD, Argon2id, Keystore wrap/unwrap)
- JVM pipeline GREEN: ./gradlew test + ktlintCheck + lint all pass; arm64 libsodium.so + libjnidispatch.so confirmed packaged in APK
- Pass-2 review fixes (2026-06-03): (1) ChatKeyStore filename token now BLAKE2b(chat-id) via injected NativeCrypto (Base32-lower, 16-byte digest/26 chars) — was a colliding polynomial fold; CryptoFactory.chatKeyStore(context) wires the shared native instance. (2) KeySources.passphraseToBytes now UTF-8-encodes the CharArray via CharBuffer→ByteBuffer with NO intermediate String, wiping both temp buffers. (3) keyFromPassphrase now zeroizes the caller CharArray (passphrase.fill(' ')) + KDoc documents consumption. (4) Aead.HEADER_SIZE now references Envelope.HEADER_SIZE (single source); Envelope.readFrame exposes the validated 8-byte header + blob, MessageCrypto.openEnvelope reuses it (no re-slice) — behavior byte-identical. (5) new JVM tests: KDF-off-calling-thread (injected tracking dispatcher), passphrase-array-wiped, readFrame header/blob; instrumented two_chat_ids_do_not_collide added.
- JVM gates re-run GREEN after fixes: test + ktlintCheck + lint.

## Remaining

- connectedAndroidTest STILL BLOCKED on 5c3ff0: INSTALL_FAILED_USER_RESTRICTED ("Install canceled by user") on both Gradle and direct `adb install`. MIUI "Install via USB" gate (M2102J20SG / Android 12) — needs the on-device install permission actually granted (MIUI often requires Mi-account sign-in + active SIM/network for this toggle, and may pop a one-time confirm dialog). APK + androidTest APK build/merge clean; ChatKeyStore collision test + Keystore + native-crypto suite ready to run once install is permitted.
- pm-plan-checker → code-review → commit → merge

## Touched files

- gradle/libs.versions.toml (lazysodium 5.1.0, jna 5.13.0, androidx-test runner/ext-junit; library + version entries)
- app/build.gradle.kts (lazysodium-android/jna @aar implementation w/ excludes, lazysodium-java/jna-jar testImplementation, androidTest deps, packaging{} META-INF excludes)
- app/src/main/kotlin/org/openwebdav/messenger/crypto/{ChatKey,NativeCrypto,LazySodiumCrypto,Aead,KeySources,MessageCrypto,CryptoFactory}.kt (new)
- app/src/main/kotlin/org/openwebdav/messenger/keystore/ChatKeyStore.kt (new; Pass-2: NativeCrypto-injected BLAKE2b filename token)
- app/src/main/kotlin/org/openwebdav/messenger/crypto/KeySources.kt (Pass-2: no-String UTF-8 encode + wipe caller CharArray)
- app/src/main/kotlin/org/openwebdav/messenger/crypto/Aead.kt (Pass-2: HEADER_SIZE references Envelope.HEADER_SIZE)
- app/src/main/kotlin/org/openwebdav/messenger/crypto/MessageCrypto.kt (Pass-2: reuse Envelope.readFrame header, no re-slice)
- app/src/main/kotlin/org/openwebdav/messenger/crypto/CryptoFactory.kt (Pass-2: chatKeyStore(context) builder sharing native)
- app/src/main/kotlin/org/openwebdav/messenger/protocol/Envelope.kt (added header() builder for AAD; Pass-2: readFrame() exposes validated header+blob, read() = readFrame().blob — transport tests stay green)
- app/src/test/kotlin/org/openwebdav/messenger/crypto/{CryptoTestSupport,AeadTest,KeySourcesTest,MessageCryptoTest,CryptoStackSpecTest}.kt (new; Pass-2: KeySourcesTest +KDF-off-thread +passphrase-wipe)
- app/src/test/kotlin/org/openwebdav/messenger/protocol/ProtocolPrimitivesTest.kt (Pass-2: +readFrame header/blob test)
- app/src/androidTest/kotlin/org/openwebdav/messenger/crypto/NativeCryptoInstrumentedTest.kt + keystore/ChatKeyStoreInstrumentedTest.kt (new; Pass-2: ChatKeyStore via CryptoFactory + two_chat_ids_do_not_collide test)

## Next step

review (pm-plan-checker → code-review). Flag for PM: instrumented gate could not execute — MIUI "Install via USB" must be enabled on 5c3ff0, then re-run `connectedAndroidTest`.

## Validation

JVM: ./gradlew test (lazysodium-java 5.1.0 via system libsodium.so.23) GREEN — 23 crypto tests + existing transport tests. ktlintCheck GREEN. lint GREEN. Device: ./gradlew connectedAndroidTest on 5c3ff0 BLOCKED by MIUI install restriction (not a code/ABI issue — arm64 .so verified in APK).

## Notes

Key decisions from PM discussion: 3 key sources, single-layer AEAD, disk app-password NEVER a content key (operator holds it), known-key = KDF(app-const ‖ chat-id) non-secret, passphrase salt deterministic from chat-id. Chat taxonomy revised: kind dm/group, access public/private, password optional. Follow-on features (NOT now): X25519 (remote private chats via published public keys), user/chat directory on disk, invite/onboarding (string+QR). Device 5c3ff0 connected for instrumented tests.

Version pin rationale: lazysodium 5.2.0 (latest common java+android) is compiled for Java 21 (class 65) and throws UnsupportedClassVersionError under the project's JDK 17 toolchain in `./gradlew test`; 5.1.x has no lazysodium-android publish above 5.1.0, so 5.1.0 is the newest matched java+android pair compiled for Java 8. JNA pinned 5.13.0 (@aar + jar both Java-8 compatible). Crypto abstracted behind NativeCrypto so the SAME Aead/KeySources run on lazysodium-java (JVM tests) and lazysodium-android (app, via CryptoFactory). known-key constant = "owdm/public-chat/v1" (non-secret, in code); salt context = "owdm/argon2id-salt/v1"; Argon2id INTERACTIVE (ops 2, mem 64 MiB) off Dispatchers.IO. MIUI install restriction on 5c3ff0 is device-side only the user can clear.

---

## How to use this file

- **Agent step 1** — read this file before doing anything else. If it says "done", do not start work without explicit PM instruction to start a new task.
- **Agent step last** — overwrite this file with the new state before stopping. Each Done item maps to one logical step that just finished; Remaining stays accurate; Next step points at the next concrete action.
- **Session restart** — re-read this file. It should be enough to continue without scrolling chat history.
- **Task complete** — copy this file to `.ai-pm/state/archive/<topic>-<YYYY-MM-DD>.md` and reset this one to a new task or to "Status: idle".
