# Threat Model

**Last reviewed:** 2026-06-07

What we protect, from whom, and how. Read by agents before planning any feature that touches a security-relevant surface (see `### Security-relevant surfaces` in `WORKFLOW.md`).

Depth matches project risk: this is a **security-bearing product** — its entire premise is that *the WebDAV disk is an untrusted server* (architecture decision 2 / Architectural constraints). The model is therefore a thorough page, not a textbook.

Owner: `pm-architect`. This is the **risk layer**; the enforceable rules live in `docs/architecture.md` `## Security constraints` (the **rule layer**), referenced from the Threats table below by stable `SCn` ID. `Last reviewed` is bumped whenever this document is drafted or updated; the auditor compares it against the merge date of the most recent security-touching feature to detect staleness.

Source of decisions: `docs/architecture.md` decisions #1–#15 (**decision #15 added 2026-06-07 — the `ui-chat-surface` first user-facing feature: app-startup wiring, Keystore-wrapped connection-config-at-rest, the `owdm1:` bearer invite, and the reserved owner-marker seam — T28–T31**) (decision #3 revised 2026-06-04 to the shared-log + change-index + retention-window model by the `sync` feature; decision #12 added 2026-06-04 — community user directory; decision #13 added 2026-06-04 — community **chat** directory; **decision #9 revised 2026-06-06 — the world-readable public-chat tier retired, "public" = community-key**) + `## Security constraints` (SC1–SC21, **SC21 added 2026-06-06 — no secrets in source/git history**); `docs/protocol/webdav-layout.md` v2 / layout generation 2 (trust-model recaps §0/§3/§5.1/§8/§9, community user directory §10, community chat directory §11); `.ai-pm/backlog.md` (join-flow / rotation / retention / host-polling notes).

> **Reviewed 2026-06-06 (`codec-dedup-and-send-hardening` feature) — no posture change.** That feature is a behaviour-preserving internal-quality consolidation (one shared parse-cursor, one shared community-directory engine, two single-source constant homes — architecture decision 14) plus one additive failure-path hardening (C8). The shared cursor consolidates SC16/SC14-relevant untrusted-byte parse code **without weakening any bound** (reject-don't-guess + bounded allocation byte-for-byte preserved); the directory-engine collapse preserves every §10/§11 invariant (collection isolation, grouping keys, the §11 `dm`-drop / SC20, secret-wipe timing). The only risk-register delta is the **strictly-safer** C8 robustness improvement, recorded as **T26** below (a potential uncaught crash converted to a typed, retryable failure). No existing `SCn` is weakened and no new confidentiality/integrity threat is introduced; `Last reviewed` bumped to 2026-06-06.

> **Reviewed 2026-06-06 (security-model correction) — net direction: TIGHTENING.** Two connected PM-approved decisions (architecture decision 9 revision + SC21): (1) the **world-readable public-chat tier is retired** — a "public" chat is now sealed under the **community key** (SC19 family), so the **disk operator loses its only remaining content-plaintext window** (public-chat plaintext) and the well-known/shared public-chat key is gone from the source; T14 (public/private confusion) is rewritten — the footgun shrinks to a *within-community over-share*, no longer an operator/world leak. (2) A new **absolute "no secrets in source / git history"** constraint (SC21, CI-enforced by gitleaks) closes a previously-uncovered source-repo leak surface (new threat **T27**); because (1) removed the one intentional in-source key, SC21 has **no carve-out**. **No `SCn` is weakened and no confidentiality/integrity guarantee is relaxed** — the operator's content visibility strictly shrinks and the in-source key surface is eliminated. `Last reviewed` stays 2026-06-06.

> **Reviewed 2026-06-06 (`x25519-identity` feature) — net direction: TIGHTENING (per-chat key isolation).** The feature touches the key-derivation security surface (`### Security-relevant surfaces` → cryptography / key management) and **closes blocker D10**: the DH-derived chat key (the fourth key source, decision 10) now **binds the chat-id** into the KDF (a new `owdm/x25519-chatkey/v2` keyed-BLAKE2b context), so **two distinct chats between the same identity pair now derive DISTINCT keys** instead of one shared pairwise key. This removes a per-chat key-reuse weakness (a key compromised — or simply shared — for one chat no longer unlocks every other chat between the same pair), tightening the **T15** (misused crypto / key-reuse) coverage over **A1/A2/A3**. The new `RemoteChatProvisioner` seam (directory peer + chat-id → derive → store) wires this fourth source into production, deriving a chat key from public keys alone with **no secret exchanged over any channel** (the box secret never leaves the device — A2/SC5; only the peer's public box key + the public chat-id are the other inputs); its native-crypto failure path degrades to a typed `Failed`, not an uncaught crash (the same C8 posture as T26). **No `SCn` is weakened and no new confidentiality/integrity threat is introduced** — a strictly-safer key-isolation improvement. `Last reviewed` stays 2026-06-06.

> **Reviewed 2026-06-07 (`ui-chat-surface` feature — the first user-facing surface) — net direction: no `SCn` weakened; four new device-local / out-of-band surfaces recorded.** This is the project's first user-facing feature (architecture decision 15); it touches **four** `### Security-relevant surfaces` items — **secret storage** (the WebDAV connection config + app-password now persist at rest), **credential distribution** (the new `owdm1:` bearer invite carries disk access + the raw chat key), a **new runtime permission** (CAMERA, for QR scanning), and **untrusted-content display** (the chat feed renders decrypted message text). It consumes every backend substrate (transport / crypto / identity / message / sync / directory) **unchanged** — no on-disk format change, no crypto change. Four new threat rows are added: **T28** (bearer invite — a plaintext token carrying the disk app-password + raw chat key; whoever obtains it gains full disk + chat access, with no per-invite revocation under Topology A — the **accepted bearer-invite limit**, mitigated only by trusted-channel sharing + the on-screen warning, never placed on the disk); **T29** (device-local storage of the WebDAV app-password + the random chat key — Keystore-wrapped, the same device-local tier as SC4/SC5, the same accepted unlocked-device limit as T16/SC17); **T30** (CAMERA permission — used only for QR scanning, optional with a paste fallback, no frames stored or sent); **T31** (displaying untrusted decrypted text — literal plain text this slice, so the SC8 markdown surface is **not yet opened**). The `random` key source (decision 9) reaches production for the first time, deriving and distributing a per-chat key with **no key on the disk** (SC1/SC3/SC4 preserved). **No existing `SCn` is weakened and no confidentiality/integrity guarantee is relaxed.** `Last reviewed` bumped to 2026-06-07.

---

## Assets

What exists in this system that has value to an attacker or matters to users.

- **A1: Chat message content** — the plaintext of private (DM and private-group) chats *and*, since the 2026-06-06 decision-9 revision, of **"public" (community-wide) chats**. All chat content is now sealed from the disk operator and from outsiders (private content is also sealed from non-member community members; "public" content is readable by all onboarded community members but never by the operator/outsiders — SC1/SC2/SC19). The product exists to keep chat plaintext hidden from the disk operator and from outsiders. **As of the `sync` feature this plaintext also persists device-local** in the Room history DB (unbounded, independent of the disk retention window — decision 3); that at-rest copy is app-private storage, never on the WebDAV disk (T16, SC17).
- **A2: Identity secret keys** — the per-user Ed25519 signing `sk` and X25519 box `sk` (decision 10). Compromise enables impersonation, forged rotations, and decryption of DH-keyed chats.
- **A3: Per-chat & community keys & passphrases** — the symmetric `ChatKey` (from passphrase / random / DH source) the **community key** (the SC19 community-wide symmetric key, which now also seals "public" chats — decision 9 revision), and any user passphrase. Compromise decrypts that chat's content within the retention window and locally.
- **A4: The shared WebDAV disk credential (app-password)** — the one credential per community/chat that authenticates every disk request (decision 2, Topology A). Compromise grants full read AND delete of the shared space (but **not** decryption — A1 stays sealed; SC3).
- **A5: Membership / social-graph metadata** — who is in which chat, who talks to whom, when, and message volume/timing/structure on disk. Deliberately **not** fully protected in the MVP (see non-goals).
- **A6: Message integrity & authorship** — the guarantee that a message's content is untampered and that it was authored by the claimed chat member (decision 11 signature; content-addressing).
- **A7: Availability of message delivery** — the ability for members to send and receive within the retention window. Bounded by a no-server, rate-limited, finite-retention design.

## Users and roles

Who legitimately uses the system and what they can access.

- **Community member** — holds the shared WebDAV disk credential (A4) plus the chat keys/passphrases (A3) for the chats they belong to. Can read, send, and (technically) delete any file in the shared space. Trust level: "knows the passphrase = insider" (flat trust, SC11).
- **Host / owner** — the member who owns the underlying Yandex account. Controls disk access and credential rotation (creates/deletes the actual app-password in Yandex ID; the per-member re-encryption is app-automated — backlog). Single point of control/trust, accepted for MVP.
- **Outsider** — anyone without the disk credential and without any chat key. Has no legitimate access; can only see what leaks at the network boundary or what a leaked invite/credential grants.

## Adversaries

Who might attack and why.

- **The disk operator (Yandex) — the PRIMARY adversary.** Honest-but-curious untrusted server: holds the WebDAV app-password (A4), sees every on-disk byte, every file name, every size and timestamp, and all write activity. The entire architecture is designed around the assumption that this party reads everything it stores. Motivation: profiling, content access, compelled disclosure.
- **Malicious / curious community insider** — a legitimate member who holds the shared credential and the chat keys for their own chats. Can read their own chats by design; may try to forge messages as another member, delete others' messages, or read chats they are not keyed into.
- **Outsider with a leaked invite / credential** — someone who obtained a bearer invite or the shared app-password out-of-band (invites are bearer tokens with no per-invite revocation on Topology A — backlog). Gains whatever the leaked material grants until rotation.
- **Network MITM** — an attacker on the path between device and disk, trying to capture the credential or ciphertext in transit, or tamper with traffic.
- **Casual attacker** — opportunistic, low-effort: a lost/found device, a shoulder-surf, an automated scan. Looks for plaintext at rest, logged secrets, default-weak config.

## Trust boundaries

Where data crosses a boundary — each one needs a defense.

| Boundary | From → To | Defense |
|---|---|---|
| Device ↔ WebDAV disk | app → untrusted cloud server | Only AEAD ciphertext + non-secret protocol files cross; keys/passphrases/plaintext NEVER cross (SC1, SC3, SC4, SC5). Transport over TLS only (SC13). Bounded reads + path-traversal rejection on every fetched/minted path (SC14, SC16). |
| App ↔ Android Keystore | app → device-local secure storage | All secret key material (chat keys, identity secret keys, passphrases) is Keystore-wrapped, device-local, never written to disk, never logged (SC4, SC5). |
| App ↔ local Room history DB | app → device-local app-private SQLite | Decrypted message history persists app-private (OS sandbox), never on the WebDAV disk, and carries no keys/passphrases (SC17; SC4/SC5). The DB file is **not** additionally app-encrypted at rest — protected on a locked device by OS storage encryption only; an unlocked in-hand device is a non-goal (T16). |
| Member ↔ member within a chat | one chat member → another, over the shared AEAD key | The shared key cannot distinguish members; per-message Ed25519 signature is the only thing that authenticates *which* member authored a message (SC15). Content-addressed append-only files resist silent in-place tampering (SC11). |
| App ↔ local connection-config store | app → device-local Keystore-wrapped app-private file | The WebDAV connection config + the secret app-password persist at rest **Keystore-wrapped** (`ConnectionConfigStore`, distinct alias), device-local, app-private, **never on the WebDAV disk, never logged** (SC4). Same device-local tier as the chat-key / identity stores; the unlocked-in-hand device is the accepted non-goal (T29/T16). |
| Out-of-band invite channel | owner → joining member (the `owdm1:` string / QR, off-disk) | The bearer invite (disk access + the raw chat key, **plain encoding not encryption** — decision 15c) is delivered **outside the disk** (copied / scanned); it is **never placed on the untrusted disk** and **never logged** (its `toString` is redacted). Confidentiality rests entirely on the chosen out-of-band channel; whoever obtains the token is in, with **no per-invite revocation** in the MVP (T28, non-goal). The on-screen bearer warning + trusted-channel sharing are the only mitigations. |

## Threats

Specific threats to this system. Each gets an ID for cross-referencing.

The **Mitigation** column references the enforceable rule in `docs/architecture.md` `## Security constraints` by its stable `SCn` ID (one-way, ID-keyed — the rule text lives there, not here, and is never copied in). The parenthetical prose is only a human hint; the `SCn` ID is the stable anchor that survives rewording of the rule.

| ID | Threat | Assets | Likelihood | Impact | Mitigation |
|---|---|---|---|---|---|
| T01 | Disk operator reads private message content directly off the disk | A1 | H | H | SC1 (E2E AEAD, ciphertext-only on disk), SC3 (app-password is not a content key) |
| T02 | Disk operator derives the content key from the app-password it holds | A1, A3, A4 | H | H | SC3 (content keys independent of disk credential), SC4 (keys Keystore-wrapped, never on disk) |
| T03 | Intra-chat impersonation — a member forges a message as another member under the shared key | A6 | M | H | SC15 (per-message Ed25519 signature, hard-reject on verify failure) |
| T04 | Tampering with on-disk message files (in-place mutation / corruption) | A6, A1 | M | M | SC1 (AEAD tamper-detection), SC15 (signature over plaintext), SC11 (append-only, content-addressed — recompute-hash-on-read) |
| T05 | Cleartext credential / ciphertext capture by a network MITM | A4, A1 | M | H | SC13 (TLS enforced, cleartext rejected) |
| T06 | Path traversal via a crafted reference (`reply-to`/`target-id`) or crafted chat-root escaping the chat folder | A1, A5, A7 | M | M | SC16 (filename-safe alphabet, grammar-validated, no `..`/`/`, reject not dereference) |
| T07 | DoS via an oversized GET or a crafted TLV field-count exhausting memory | A7 | M | M | SC14 (bounded reads: 1 MiB GET cap + field-count cap, typed reject) |
| T08 | Malicious Markdown in a decrypted message (HTML injection, remote-image tracking/SSRF beacon, auto-navigation) | A1, A5 | M | M | SC8 (no HTML, no remote images, scheme allowlist, no autolink, tap-only navigation) |
| T09 | Compression side-channel (CRIME/BREACH-class length leak) co-compressing a secret with attacker-influenced data | A1 | L | M | SC6 (per-message independent compression), SC7 (bounded decompression) |
| T10 | Sealed-box rotation forgery — a sender-unauthenticated sealed payload spoofs a key rotation | A2, A3, A4 | M | H | SC12 (sealed-box is sender-unauthenticated → MUST be Ed25519-signed), SC15 (Ed25519 detached signature establishes authorship) |
| T11 | Untrusted decompression bomb (zip-bomb) inflating to exhaust device memory | A7 | L | M | SC7 (decompression size bound, error path not crash) |
| T12 | Secret material leaking to disk or logs (passphrase, derived key, identity sk) | A2, A3 | M | H | SC4 (chat keys Keystore-wrapped, never on disk/log), SC5 (identity sk Keystore-wrapped, never on disk/log). The `identity-store-io-dispatch` feature moved `IdentityStore.loadOrCreate()` to `Dispatchers.IO` and replaced `synchronized` with a coroutines `Mutex`; the key generation, Keystore wrapping, zeroization, and cross-process `FileLock` logic are unchanged — no new leak surface introduced |
| T13 | A member reads a chat they are not keyed into, by reading the shared disk space | A1 | M | M | SC1 (per-chat AEAD key — not keyed = ciphertext only), SC11 (inbox split is read-efficiency, not access control — confidentiality rests on the key) |
| T14 | User over-shares in a "public" (community-wide) chat — posts something meant for a few people into a chat **all onboarded community members** can read | A1, A5 | M | L | SC2 (a "public" chat is community-barrier confidential — sealed from the disk operator and outsiders under the community key, SC19 — but readable by **all** community members, NOT private to specific people; UI warns "all community members can read this" and nudges to a private chat). **Footgun shrank 2026-06-06** (decision 9 retired the world-readable public-chat tier): there is **no longer a chat tier readable by the disk operator or by outsiders**, so this is a *within-community over-share* (community members vs specific members), **not** an operator/world plaintext leak as before |
| T15 | Hand-rolled or misused crypto introduces a break (nonce reuse, weak KDF, **key reuse across contexts**, unverified tag) | A1, A2, A3 | L | H | SC9 (audited primitives only), SC1 (XChaCha20-Poly1305 AEAD), SC10 (no `!!` on crypto paths — null-safe boundary). **Tightened 2026-06-06** (`x25519-identity`, blocker D10 fixed): the DH-derived chat key (decision 10) now **binds the chat-id** (a new `owdm/x25519-chatkey/v2` keyed-BLAKE2b context with explicit `0x1F` separator), so **two chats between the same identity pair derive distinct keys** — closing a per-chat key-reuse weakness (one shared pairwise key for every chat between a pair). Both members still derive the same key from **public inputs only**, no secret exchanged over any channel; the box secret stays Keystore-bound (A2/SC5). Net **tightening**, no `SCn` weakened |
| T16 | Decrypted message history readable at rest in the local Room DB on a lost / unlocked device | A1 | M | M | SC17 (device-local app-private storage, never on the WebDAV disk; full-DB-at-rest encryption is an **accepted limitation** — relies on OS device-lock + storage encryption, see non-goal *Physical access to an unlocked device*), SC4/SC5 (chat & identity keys stay Keystore-wrapped, never in the history DB) |
| T17 | Longer disk-operator exposure: messages persist on the shared disk for a retention window (ciphertext + size/timing metadata observable for the whole window, vs a delete-after-read model) | A1, A5 | M | M | SC1 (ciphertext only — content stays sealed for the whole window; the longer window exposes **no plaintext**), SC3 (app-password is not a content key). Size/timing **metadata** over the window is the accepted *Metadata visible to the disk operator* non-goal (A5), not a content leak |
| T18 | Per-member change index (`changes/<member-index-id>/`) leaks per-member activity, per-chat change timing/volume, and membership-shape metadata to the disk operator | A5 | M | L | SC1 (change entries carry **no** message body / key / plaintext — cursor coordinate only; content stays sealed in `log/`). Same metadata class as the v1 per-recipient inbox layout — **not a new secret-exposure surface**; the metadata itself is the accepted *Metadata visible to the disk operator* non-goal (A5) |
| T19 | A member polls faster than the (future) community polling floor, degrading the shared disk's 429 budget for everyone (cooperative honest-client assumption) | A7 | L | M | **No enforced mitigation — ASSUMPTION / non-goal.** The host-governed polling floor is honest-client only (over-polling is hard to attribute under one shared credential, SC11); a host-enforced floor is a future community feature. Recorded as the *Cooperative polling-floor enforcement* non-goal, not an enforced guarantee |
| T20 | Directory entry impersonation by display-name — a member publishes an entry with a duplicate display-name and their own keys; the entry is validly self-signed, but a name does not prove the person behind it | A5, A6 | M | M | SC18 (entries are Ed25519-signed — keys bound to keys, tamper-evident; the claimed signing key is itself signed) + the **accepted limitation** that name↔human binding is the deferred QR safety-number, recorded as the *Directory name↔human binding deferred to QR safety-number* non-goal (self-signing proves authorship, NOT identity) |
| T21 | Community-key compromise — a leaked/shared community key makes the whole directory readable (names + public keys) to whoever holds it | A5 | M | M | SC19 (community key is a community-wide symmetric key independent of the disk credential, never on disk — by design it bounds *who* reads the directory to **onboarded community members**, the "community barrier" tier analogous to SC2's known-key tier). The community key is a **shared/bearer secret**; per-member revocation is the future key-rotation feature — recorded as the *Community-key is a bearer secret; no per-member directory revocation* non-goal |
| T22 | Directory metadata exposure — the disk operator sees the `directory/` collection structure, entry count, sizes, and write timing (roughly how many members and when they publish) even though names/keys are AEAD-sealed | A5 | M | L | SC19 + SC1 (entry **content** — names/keys — stays AEAD-sealed; only the collection's structure/count/size/timing is observable). Same A5 metadata class as T17/T18 — **not** a new content-leak surface; the metadata itself is the accepted *Metadata visible to the disk operator* non-goal (A5) |
| T23 | Chat-descriptor spoofing / unauthorized supersede — under flat trust any member can publish or supersede a chat descriptor for a chat-id they do not own (a wrong title, a flipped `access`); the descriptor's signature is tamper-evidence on the bytes + authorship of *this version*, NOT chat-ownership authority (the supersede grouping key is the chat-id, not the signature's verification key) | A5, A6 | M | M | SC18 (chat-descriptor entries Ed25519-signed, hard-reject on `-1`; `signing-pubkey` inside the signed range — tamper-evidence + authorship of the version) + SC20 (DMs never published / `dm` hard-reject on read) + the **accepted limitation** that an authoritative chat-ownership marker is deferred, recorded as the *Chat-descriptor authorship ≠ chat-ownership authority; ownership marker deferred* non-goal (parallel to T20's name↔human deferral) |
| T24 | Private-group existence/title exposure to community members — by the PM scope decision a private group's existence + title are readable by ALL onboarded members (the community-barrier tier, sealed from the disk operator under the community key); only the chat content key stays out of reach | A5 | M | M | SC19 (community key bounds *who* reads to onboarded members — the community-barrier tier, independent of the disk credential, never on disk) + SC1 (existence/title sealed from the disk operator; the chat **content key is NEVER in the directory** — joining still needs the key out-of-band). An accepted, recorded design boundary (analogous to T21), the *Private-group existence/title is community-barrier-visible; content key never in the directory* non-goal |
| T25 | Chat-directory metadata exposure to the disk operator — the operator sees the `chat-directory/` collection structure, entry count, sizes, and write timing (roughly how many chat descriptors exist and when they publish) even though chat-ids/kinds/access/titles are AEAD-sealed | A5 | M | L | SC19 + SC1 (entry **content** — chat-id/kind/access/title — stays AEAD-sealed; only the collection's structure/count/size/timing is observable). Same A5 metadata class as T17/T18/T22 — **not** a new content-leak surface; the metadata itself is the accepted *Metadata visible to the disk operator* non-goal (A5) |
| T26 | A native AEAD seal failure mid-publish (a spurious libsodium `crypto_aead_..._encrypt` failure surfaced as an uncaught `IllegalStateException`) crashes the directory/chat-directory **publish** path instead of returning a recoverable typed failure | A7 | L | L | SC14 (untrusted-input robustness — bounded, typed reject/failure paths, never an uncaught crash; the `codec-dedup-and-send-hardening` feature, 2026-06-06, maps the native seal `IllegalStateException` at the two directory publish boundaries to the existing typed `Failed` outcome — strictly safer, the catch is narrow so unrelated bugs still surface). **Robustness/availability hardening, no confidentiality or integrity change** — the parse/seal/wipe semantics are byte-for-byte unchanged (architecture decision 14); no `SCn` weakened. The equivalent message-build-path hardening is deferred to the future send/UI feature (plan → Out of scope)
| T27 | Secret material (keys, credentials, passphrases) committed to the source tree or git history leaks via the (public, AGPL-3.0) repository | A2, A3, A4 | L | H | SC21 (no secret material in the source tree or git history — **absolute**, no in-source-key carve-out after decision 9 retired the world-readable public-chat key; secrets live only in the Android Keystore / the out-of-band invite channel / gitignored local config; **CI-enforced by a gitleaks secret-scanner gate** in `.github/workflows/pr-checks.yml`). **Current actual exposure is clean** (a 2026-06-06 scan found no hardcoded keys, no committed credential files; `local.properties` gitignored; debug-only signing, so no release keystore/credentials in the tree) — the constraint + scanner keep it that way as the repo goes open-source. Likelihood L reflects the clean current state + the CI gate; impact H because a leaked disk credential or chat/community key voids confidentiality for whatever it unlocks |
| T28 | Bearer invite leak — the `owdm1:` invite is a plaintext token carrying the disk app-password + the raw chat key; whoever obtains it (a screenshot, a shoulder-surf of the QR, a forwarded message, messenger history) gains full disk + chat access, "forever" under Topology A (no per-invite revocation) | A1, A3, A4 | M | H | SC4 (the invite is never written to the disk and never logged — it lives only in the out-of-band channel and, transiently, in memory; the decoded token's `toString` is redacted) + the **accepted limitation** that the invite is a bearer token shared out-of-band with **no per-invite revocation** under Topology A — recorded as the *Individual invite revocation* non-goal; the mitigations are **procedural** (share only over a trusted channel) + the **on-screen warning** ("anyone who gets this invite can read and write this chat and use the disk — share it only with people you trust"). The invite is **plain encoding, not encryption** (decision 15c), so it carries no cryptographic protection of its own; this is the inherent Topology-A reality, accepted for the MVP |
| T29 | Offline extraction of the WebDAV app-password + the random chat key from a lost / unlocked / rooted device's local stores | A3, A4 | M | M | SC4 (the connection config + app-password are Keystore-wrapped device-local via `ConnectionConfigStore`, never on the WebDAV disk / never logged), SC5 family (the chat key stays in the Keystore-wrapped `ChatKeyStore`). Same device-local tier and **same accepted unlocked-device limit** as T16/SC17 — Keystore-wrapping + OS device-lock + storage encryption protect a **locked** device; an unlocked, in-hand device is the recorded *Physical access to an unlocked device* non-goal, not an app-level guarantee |
| T30 | The CAMERA permission (added for QR-scan join) is abused — surreptitious capture, or an over-broad permission footprint | A5 | L | L | **Minimal-surface design, no enforced SCn beyond the manifest declaration:** CAMERA is used **only** for the optional QR-scan join path; the **paste path is the mandated fallback**, so the permission is declared **`required="false"`** (`<uses-feature camera.any required="false"/>`) — the app stays installable and fully usable on camera-less devices and the permission can be denied with no loss of function. **No camera frames are stored or sent** — the scanner decodes a QR string in-memory and discards the frames; the only output is the decoded invite string handed to the reject-don't-guess decoder. An accepted, recorded minimal-permission boundary (no background camera use, no other permission added) |
| T31 | Malicious content in a decrypted message rendered in the feed (HTML injection, remote-image tracking/SSRF beacon, auto-navigation, markup exploit) | A1, A5 | L | L | SC8 (no HTML, no remote images, scheme allowlist, no autolink, tap-only navigation) — **realized deferred-safe in this slice:** the feed renders the message body as **literal plain text** (Markdown characters shown as-is, links not tappable, nothing auto-loaded), so the SC8 markdown surface is **not yet opened**. The richer rendering layer (the Markdown subset + its untrusted-input attack surface) is a separate later slice that will land the active SC8 enforcement; until then the literal-plain-text default carries no rendering attack surface at all (decision 5 / decision 15) |

Likelihood and Impact: L / M / H

> **Which shipped feature realizes each threat's mitigation** is tracked one place — the `### Realized by — feature → SCn / Threat traceability` table in `docs/architecture.md` `## Security constraints` (added 2026-06-04, audit-2026-06-04 Note 1). That table maps each merged substrate feature (`webdav-transport`, `crypto`, `identity`, `message-model`, `sync`, `directory`, `chat-directory`) to the `SCn` constraints it realizes and the `Tnn` rows above it addresses. The link is one-way and ID-keyed — this threat-model is not edited per realizing feature; the realized-by mapping lives next to the constraint definitions to avoid duplication. A future security feature reads that table to see what a surface already defends before extending it.

## What we explicitly do NOT protect against

Be honest about out-of-scope threats — without this list, scope creeps. These are load-bearing for this project: each is a deliberate MVP boundary, not an oversight.

- **Metadata visible to the disk operator** — who is in which chat, who writes when, message counts, sizes, file/folder structure, and the resulting traffic-analysis. This is the same metadata class the generation-2 shared-log + per-member change-index layout exposes (per-member activity, per-chat change timing/volume, membership shape — **T18**), that the retention window lengthens the exposure of (**T17**), and that the **community user directory** collection exposes (its structure, entry count, sizes, and write timing — roughly how many members and when they publish — **T22**), and that the **community chat directory** collection likewise exposes (its structure, entry count, sizes, and write timing — roughly how many chat descriptors exist and when they publish — **T25**); none of these is a new secret-exposure surface (the change index carries cursor coordinates and the directory entries carry only AEAD ciphertext, never content). Both directories' content (the user directory's names/keys; the chat directory's chat-ids/kinds/access/titles) is sealed under the community key (T22/T25, SC19/SC1); only the collections' shape/timing leaks — exactly this metadata non-goal. Full metadata hiding (padding, cover traffic, name randomization) is a separate, larger concern, out of MVP (A5; backlog).
- **Message deletion by any member** — under one shared credential (Topology A, SC11) every member can `DELETE` any file. AEAD gives tamper-*detection* of content, not deletion resistance. Deletion resistance (signatures over a witnessed/replicated log) is future work.
- **Forward secrecy** — a leaked passphrase or chat key exposes that chat's history within the retention window (and locally, unbounded). The double-ratchet / per-message-key path is deferred (decision 9; backlog).
- **Individual invite revocation** — invites are bearer tokens; whoever scans one is a member forever. Removing one person requires the rotate-with-auto-replace mechanism (future, needs X25519 + host action); until then there is no per-invite revocation on Topology A (backlog).
- **Directory name↔human binding deferred to QR safety-number (T20)** — a community directory entry is **self-signed**: the signature proves the entry was authored by the holder of the carried signing key (keys bound to keys, tamper-evident — SC18), but it does **NOT** prove that a display-name belongs to a particular person. A member can publish a duplicate display-name with their own valid keys. Binding "this entry is really that person" is the **QR safety-number verification UI** (a later feature, computed over the public identity keys the directory surfaces — decision 10 fingerprint). Host-attested entries (a community host counter-signing each member's entry) were considered and rejected for the MVP per the PM trust decision; the signed-entry format does not preclude a later host counter-signature.
- **Community-key is a bearer secret; no per-member directory revocation (T21)** — the directory is readable by anyone holding the **community key**, a shared/bearer secret distributed at onboarding. This is by design: the community key bounds *who* can read the directory to **onboarded members** (the "community barrier" trust tier — the **same** community-barrier tier a "public" chat now uses, SC2/SC19, post the 2026-06-06 decision-9 revision), independent of the disk credential and never on disk (SC19). There is **no per-member revocation** of directory access in the MVP; removing one member's read access requires re-keying the community — the future key-rotation feature (same class as *Individual invite revocation* above; backlog rotation).
- **Chat-descriptor authorship ≠ chat-ownership authority; ownership marker deferred (T23)** — a community **chat** directory descriptor is **self-signed**: the signature proves the descriptor version was authored by the holder of the carried signing key (tamper-evidence on the bytes + authorship of *this version* — SC18), but it does **NOT** prove **chat-ownership authority**. Because the supersede grouping key is the **chat-id** (not the signature's verification key, unlike the §10 user directory which groups by signing-pubkey), under flat trust (SC11) **any member can sign and publish a competing descriptor for any chat-id** — a wrong title or a flipped `access` — and the latest signed `version-counter` wins. The descriptor is **advisory discovery metadata, not authoritative chat ownership**. An **authoritative chat-ownership marker** (a creator-signed / host-attested "this descriptor is authoritative for this chat-id") is **deferred** to the future community / rotation features; the signed-descriptor format does not preclude a later authority counter-signature. Parallel to the *Directory name↔human binding deferred* non-goal (T20) — both are accepted flat-trust limits, not oversights.
- **Private-group existence/title is community-barrier-visible; content key never in the directory (T24)** — by the PM scope decision (2026-06-04), a private **group**'s **existence + title** are discoverable by **all onboarded community members** (the community-barrier tier — sealed under the community key, never visible to the disk operator, SC19/SC1), the same trust tier as the user directory (T21). This is by design: discovery within a community needs the group to be listed. The private group's **content key is NEVER placed in the directory** — a member who discovers a private group still needs the key **out-of-band** (the future onboarding/invite feature) to read or join it; the directory surfaces existence + metadata only. **DMs (1:1 chats) are excluded entirely** (never published, `dm` hard-reject on read — SC20) to avoid leaking the community's social graph. Removing one member's discovery access requires re-keying the community — the future key-rotation feature (same class as *Community-key is a bearer secret* above).
- **A member offline longer than the retention window losing messages** — the disk holds only a bounded retention window (e.g. 2–4 weeks, configurable); a member offline beyond it loses the oldest messages. Unavoidable without a server and with finite disk (backlog retention model). The window also lengthens the disk operator's exposure to ciphertext + size/timing metadata vs a delete-after-read model — **content stays sealed throughout** (T17, SC1); only the metadata window grows. (Window pruning is a deferred follow-on; the `sync` feature only reserves the window — `docs/protocol/webdav-layout.md` §1.4.)
- **Cooperative polling-floor enforcement (T19)** — the host sets a community-wide minimum polling interval, but enforcement is honest-client: a member can over-poll and hurt the shared 429 budget. Over-polling is hard to attribute under one shared credential (SC11). This is a cooperative **assumption, not an enforced guarantee** — a host-governed floor is a future community feature (backlog host-polling-floor).
- **A trusted member reading their own chats** — by design, a keyed member reads the chats they belong to. This is the intended function, not a threat.
- **Physical access to an unlocked device** — once a device is unlocked, the local Room history (now the unbounded device-local plaintext store — decision 3) and in-memory keys are readable. The history DB is app-private and never on the WebDAV disk, but is **not** additionally app-encrypted at rest (T16, SC17); device-lock and OS-level storage encryption are the user's responsibility. We do not defend an unlocked, in-hand device; full at-rest DB encryption is candidate future hardening.
- **Nation-state / endpoint compromise** — a compromised OS, a malicious keyboard, a rooted device, or a state-level adversary subverting the Keystore/CSPRNG is out of scope. We trust the platform's Keystore and CSPRNG.

---

## Review

Bump the **Last reviewed** date at the top whenever this document is drafted or updated. Revisit this document when:

- A feature touches a `### Security-relevant surfaces` item (see `WORKFLOW.md`)
- New user role added
- Security incident occurs
